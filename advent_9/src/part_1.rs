use crate::read_file_str;


pub fn run(input: &str) -> u32 {
    return 0;
}

#[cfg(test)] 
mod tests {
    use super::*;

    #[test]
    fn data_1() {
        let result = run("input_test.txt");
        assert_eq!(result, 13)
    }
}